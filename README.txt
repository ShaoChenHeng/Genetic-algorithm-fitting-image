图片文件的文件名model，格式必须是.jpg，这个可以通过画图和其他工具直接将非.jpg格式的文件另存为.jpg。放在根目录。

输出图片保存在results文件夹。

parameter是参数文本，默认第一行15000，第二行100，第三行256。可以自行更改。

第一行是迭代次数，迭代次数越多结果越像，运行时间也越长。
第二行是隔代输出的代数，100就是隔100代输出一次。
第三行是染色体数量，越大收敛的就越慢，最后结果也会越像，运行时间也越长。


